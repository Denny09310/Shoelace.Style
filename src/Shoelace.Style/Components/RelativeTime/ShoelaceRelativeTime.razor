@namespace Shoelace.Style.Components
@inherits ShoelaceComponentBase

<sl-relative-time id="@Id"
                  class="@Class"
                  style="@Style"
                  date="@(Date.ToString("o", CultureInfo.InvariantCulture))"
                  format="@Format"
                  numeric="@Numeric"
                  sync="@Sync"
                  @attributes="AdditionalAttributes"></sl-relative-time>