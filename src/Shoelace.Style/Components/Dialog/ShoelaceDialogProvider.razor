@namespace Shoelace.Style.Components
@inject IDialogService DialogService;

@foreach (var option in Options)
{
    <ShoelaceDialog @key="option" Label="@option.Label" Open="@option.Open" OnRequestClose="@(() => HandleDialogClosed(option))">
        @option.Content
    </ShoelaceDialog>
}
