@namespace Shoelace.Style.Components
@inherits ShoelaceComponentBase

<sl-format-date id="@Id"
                class="@Class"
                style="@Style"
                date="@(Date?.ToString("o", CultureInfo.InvariantCulture))"
                day="@Day"
                era="@Era"
                hour="@Hour"
                hour-format="@HourFormat"
                minute="@Minute"
                month="@Month"
                second="@Second"
                time-zone="@TimeZone"
                time-zone-name="@TimeZoneName"
                weekday="@Weekday"
                year="@Year"
                @attributes="AdditionalAttributes"></sl-format-date>